### Sources
{{#each sources}}
- {{source_name}} ({{source_id}}) – {{access_level}}, retrieved {{retrieved_at}}.
{{/each}}

Include direct URLs for public data when available. Reference dataset notes from `SOURCE_REGISTRY.csv` for license reminders.
